
spring:
  datasource:
    url: jdbc:mysql://localhost:13306/nac_korea?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: root

  jpa:
    show-sql: true

jwt:
  secret: dev-local-secret-key-for-development-only-not-used-in-production

logging:
  level:
    com.nackorea: DEBUG
    org.hibernate.SQL: DEBUG