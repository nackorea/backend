
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/nac_korea?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: nac_user
    password: nackorea123

  jpa:
    show-sql: false
    hibernate:
      ddl-auto: validate

logging:
  level:
    com.nackorea: INFO
    root: WARN